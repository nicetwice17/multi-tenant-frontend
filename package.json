{
  "name": "multi-tenant-frontend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "themes/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/player-shell",
    "build": "npm run build --workspace=apps/player-shell",
    "test": "npm run test --workspace=apps/player-shell",
    "lint": "npm run lint --workspace=apps/player-shell"
  }
}
